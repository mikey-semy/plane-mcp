# Использование plane-mcp с GitHub Copilot в VSCode

## 🎯 Внешний вид и использование

### Подключение MCP сервера

После настройки `mcp.json`, при открытии VSCode:

1. **В статус-баре** (внизу справа) появится индикатор MCP серверов
2. **В панели Copilot** появится секция с доступными инструментами
3. **В чате Copilot** можно вызывать инструменты командами

### Визуальная индикация

```
┌─────────────────────────────────────────┐
│  VSCode Status Bar (внизу)              │
│  🔌 MCP: plane (47 tools)  🟢           │
└─────────────────────────────────────────┘
```

**Статусы:**
- 🟢 **Зелёный** - сервер подключён и работает
- 🟡 **Жёлтый** - подключение...
- 🔴 **Красный** - ошибка подключения

### Панель Copilot

В боковой панели Copilot (Ctrl+Shift+I или Cmd+Shift+I):

```
┌───────────────────────────────────────┐
│  GitHub Copilot Chat                  │
├───────────────────────────────────────┤
│  📋 Available Tools                   │
│                                       │
│  📦 plane (47 tools)                  │
│    👤 User                            │
│       • get_current_user              │
│       • get_workspace_members         │
│                                       │
│    📊 Projects                        │
│       • get_projects                  │
│       • create_project                │
│                                       │
│    📝 Issues                          │
│       • list_project_issues           │
│       • get_issue                     │
│       • create_issue                  │
│       • update_issue                  │
│       • ...                           │
└───────────────────────────────────────┘
```

## 💬 Примеры использования в чате

### 1. Получение информации о проектах

**Вы пишете в Copilot Chat:**
```
Покажи все проекты в Plane
```

**Copilot:**
- Видит доступный инструмент `get_projects`
- Автоматически вызывает его
- Возвращает список проектов:

```markdown
📊 Найдено проектов: 3

1. **ProfitoolStore** (PROFIT)
   - Статус: активен
   - Задач: 45
   
2. **Mobile App** (MOBILE)
   - Статус: активен
   - Задач: 23
   
3. **Backend API** (API)
   - Статус: в процессе
   - Задач: 67
```

### 2. Создание задачи

**Вы:**
```
Создай задачу "Исправить баг с авторизацией" в проекте PROFIT
```

**Copilot:**
- Вызывает `create_issue`
- Передаёт параметры
- Возвращает результат:

```markdown
✅ Задача создана!

**PROFIT-46**: Исправить баг с авторизацией
- Статус: Backlog
- Приоритет: High
- Ссылка: https://plane.equiply.ru/profitool-store/projects/xxx/issues/46
```

### 3. Получение задачи по ID

**Вы:**
```
Покажи детали задачи PROFIT-46
```

**Copilot:**
- Распознаёт читаемый ID (PROFIT-46)
- Вызывает `get_issue_using_readable_identifier`
- Показывает полную информацию:

```markdown
📋 **PROFIT-46**: Исправить баг с авторизацией

**Описание:**
При входе в систему пользователи получают ошибку 401...

**Статус:** In Progress
**Приоритет:** High
**Исполнитель:** @mike
**Метки:** bug, urgent
**Создан:** 2024-11-08
**Обновлён:** 2024-11-08

**Комментарии (3):**
1. @alex: Баг воспроизводится на...
2. @mike: Нашёл причину в...
3. @alex: Отлично, жду фикса
```

### 4. Работа с модулями

**Вы:**
```
Какие модули есть в проекте PROFIT?
```

**Copilot:**
```markdown
📦 Модули проекта PROFIT:

1. **Authentication** (AUTH)
   - Задач: 12
   - Статус: 75% завершено
   - Дедлайн: 2024-12-01
   
2. **User Management** (USER)
   - Задач: 8
   - Статус: 100% завершено ✅
   
3. **Payments** (PAY)
   - Задач: 15
   - Статус: 45% завершено
   - Дедлайн: 2024-12-15
```

### 5. Добавление worklogs

**Вы:**
```
Добавь 3 часа работы на задачу PROFIT-46 с описанием "Исправлен баг авторизации"
```

**Copilot:**
```markdown
⏱️ Worklog добавлен!

**Задача:** PROFIT-46
**Время:** 3 часа
**Описание:** Исправлен баг авторизации
**Дата:** 2024-11-08
```

## 🔄 Разница между stdio и SSE

### stdio (локальный)

```json
{
  "servers": {
    "plane-workspace1": {
      "command": "npx",
      "args": ["-y", "@mikey-semy/plane-mcp"],
      "env": {
        "PLANE_WORKSPACE_SLUG": "workspace1"
      }
    },
    "plane-workspace2": {
      "command": "npx",
      "args": ["-y", "@mikey-semy/plane-mcp"],
      "env": {
        "PLANE_WORKSPACE_SLUG": "workspace2"
      }
    }
  }
}
```

**Преимущества:**
- ✅ Несколько workspace одновременно
- ✅ Разные API ключи для разных workspace
- ✅ Локальный контроль

**В Copilot видно:**
```
📦 plane-workspace1 (47 tools) 🟢
📦 plane-workspace2 (47 tools) 🟢
```

Можно переключаться между ними!

### SSE (удалённый)

```json
{
  "servers": {
    "plane": {
      "url": "https://mcp.plane.equiply.ru:9000/sse"
    }
  }
}
```

**Преимущества:**
- ✅ Не нужен Python/uv локально
- ✅ Быстрое подключение
- ✅ Централизованное управление

**Недостатки:**
- ❌ Только один workspace (тот, что на сервере)
- ❌ Нужен доступ к серверу

**В Copilot видно:**
```
📦 plane (47 tools) 🟢
```

Всё работает одинаково, просто подключение другое!

## 🎨 Команды в Copilot

### Прямые вызовы инструментов

Copilot автоматически понимает естественный язык и вызывает нужные инструменты:

- "Покажи проекты" → `get_projects`
- "Создай задачу..." → `create_issue`
- "Список задач проекта X" → `list_project_issues`
- "Добавь метку Y к задаче Z" → `update_issue`
- "Покажи мои worklogs" → `get_issue_worklogs`

### Сложные запросы

**Вы:**
```
Покажи все задачи в модуле Authentication проекта PROFIT, 
которые в статусе "In Progress"
```

**Copilot:**
- Вызывает `list_modules` для получения ID модуля "Authentication"
- Вызывает `list_module_issues` для получения задач модуля
- Фильтрует по статусу "In Progress"
- Форматирует результат

```markdown
🔍 Найдено задач: 3

1. **PROFIT-46**: Исправить баг с авторизацией
   - Исполнитель: @mike
   - Приоритет: High
   
2. **PROFIT-47**: Добавить 2FA
   - Исполнитель: @alex
   - Приоритет: Medium
   
3. **PROFIT-48**: Обновить токены
   - Исполнитель: @mike
   - Приоритет: Low
```

## 🚀 Продвинутое использование

### Создание отчётов

**Вы:**
```
Создай отчёт по всем задачам проекта PROFIT за последнюю неделю
```

**Copilot:**
- Получает все задачи
- Фильтрует по дате
- Группирует по статусам
- Считает статистику
- Генерирует markdown отчёт

### Массовые операции

**Вы:**
```
Добавь метку "urgent" ко всем задачам с приоритетом High в проекте PROFIT
```

**Copilot:**
- Получает задачи с приоритетом High
- Для каждой вызывает `update_issue`
- Добавляет метку
- Отчитывается о результатах

## 💡 Советы по использованию

1. **Будьте конкретны**: "Покажи задачи проекта PROFIT" лучше чем "Покажи задачи"
2. **Используйте читаемые ID**: PROFIT-46 вместо UUID
3. **Комбинируйте запросы**: Copilot может выполнить несколько инструментов последовательно
4. **Просите форматирование**: "Покажи в виде таблицы", "Группируй по статусам"

## 🔧 Troubleshooting

### Сервер не подключается (SSE)

```
🔴 plane (connection failed)
```

**Проверьте:**
1. Доступность URL: `curl https://mcp.plane.equiply.ru:9000/sse`
2. Firewall не блокирует порт 9000
3. SSL сертификат валиден (для https)

### Сервер не запускается (stdio)

```
🔴 plane-workspace1 (failed to start)
```

**Проверьте:**
1. uv установлен: `uv --version`
2. Переменные окружения установлены
3. API ключ валиден
4. Workspace slug правильный

### Инструменты не работают

**Ошибка 401 Unauthorized:**
- Проверьте PLANE_API_KEY
- Токен мог истечь - создайте новый

**Ошибка 404 Not Found:**
- Проверьте PLANE_WORKSPACE_SLUG
- Workspace может быть переименован

## 📚 Дополнительная информация

- **Все 47 инструментов**: См. README.md
- **Plane API**: https://docs.plane.so/
- **MCP Protocol**: https://modelcontextprotocol.io/
- **GitHub**: https://github.com/mikey-semy/plane-mcp
