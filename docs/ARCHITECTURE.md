# Архитектура plane-mcp

## 🏗️ Два режима работы

### Режим 1: stdio (Локальный)

```
┌──────────────────┐
│   VSCode/Claude  │
│     Desktop      │
└────────┬─────────┘
         │ stdio (stdin/stdout)
         │ npx @mikey-semy/plane-mcp
         ↓
┌────────────────────┐      HTTPS
│  Python MCP Server │ ────────────→ ┌──────────────┐
│   (FastMCP)        │ ←──────────── │  Plane API   │
│   + uv + httpx     │               │  plane.so    │
└────────────────────┘               └──────────────┘
  Локально на машине                  Удалённый API
  пользователя
```

**Запуск:**
```json
{
  "servers": {
    "plane": {
      "command": "npx",
      "args": ["-y", "@mikey-semy/plane-mcp"],
      "env": {
        "PLANE_API_KEY": "plane_xxx",
        "PLANE_WORKSPACE_SLUG": "workspace-1",
        "PLANE_API_HOST_URL": "https://api.plane.so/"
      }
    }
  }
}
```

**Поток данных:**
1. VSCode → npx запускает Node.js wrapper
2. Node.js → запускает `uv run plane-mcp` (Python)
3. Python MCP сервер → общается через stdio
4. MCP инструменты → вызывают Plane API (httpx)
5. Ответы возвращаются обратно через stdio

### Режим 2: SSE (Удалённый HTTP сервер)

```
┌──────────────────┐
│   VSCode/Claude  │
│     Desktop      │
└────────┬─────────┘
         │ HTTP/SSE
         │ https://mcp.plane.equiply.ru:9000/sse
         ↓
┌────────────────────┐      HTTPS
│  Python MCP Server │ ────────────→ ┌──────────────┐
│   (FastMCP)        │ ←──────────── │  Plane API   │
│   в Docker         │               │  plane.so    │
└────────────────────┘               └──────────────┘
  Развёрнут на сервере               Удалённый API
  (Dokploy/DigitalOcean)
```

**Запуск:**
```json
{
  "servers": {
    "plane": {
      "url": "https://mcp.plane.equiply.ru:9000/sse"
    }
  }
}
```

**Поток данных:**
1. VSCode → HTTP запрос к MCP серверу
2. SSE соединение устанавливается
3. MCP инструменты → вызывают Plane API (httpx)
4. Ответы возвращаются через SSE

## 📦 NPM Package структура

```
@mikey-semy/plane-mcp/
├── bin/
│   └── plane-mcp.js          # Node.js обёртка
├── scripts/
│   └── install-python.js     # Установка Python зависимостей
├── src/
│   └── plane_mcp/
│       ├── server.py         # FastMCP сервер
│       ├── schemas/          # Pydantic модели
│       └── tools/            # 47 MCP инструментов
├── package.json              # NPM конфигурация
├── pyproject.toml            # Python зависимости
└── uv.lock                   # Locked зависимости
```

**При установке:**
1. `npm install -g @mikey-semy/plane-mcp`
2. postinstall → `node scripts/install-python.js`
3. Проверяет наличие `uv`
4. Запускает `uv sync` для установки Python пакетов
5. Создаёт `.venv` с зависимостями

**При запуске:**
1. `plane-mcp` → вызывает `bin/plane-mcp.js`
2. Node.js проверяет `uv --version`
3. Запускает `uv run plane-mcp` с переменными окружения
4. Python сервер стартует в режиме stdio
5. Готов к работе!

## 🔄 Сравнение подходов

| Характеристика | stdio (Локальный) | SSE (Удалённый) |
|---------------|-------------------|-----------------|
| **Установка** | Требует uv + Node.js | Только VSCode |
| **Workspace** | Несколько одновременно | Один (на сервере) |
| **API ключи** | Свои у каждого | Настроены на сервере |
| **Скорость** | Быстро (локально) | Зависит от сети |
| **Автономность** | Работает офлайн | Требует интернет |
| **Обновления** | npm update | Обновить Docker образ |
| **Безопасность** | Ключи локально | Ключи на сервере |
| **Использование** | Персональное | Командное |

## 🎯 Рекомендации

### Для личного использования:
✅ **stdio** - больше гибкости, можно работать с разными workspace

```json
{
  "servers": {
    "plane-work": {
      "command": "npx",
      "args": ["-y", "@mikey-semy/plane-mcp"],
      "env": {
        "PLANE_WORKSPACE_SLUG": "work-projects"
      }
    },
    "plane-personal": {
      "command": "npx",
      "args": ["-y", "@mikey-semy/plane-mcp"],
      "env": {
        "PLANE_WORKSPACE_SLUG": "personal"
      }
    }
  }
}
```

### Для команды/организации:
✅ **SSE** - централизованный сервер, единая конфигурация

1. Развернуть на сервере (Docker)
2. Настроить workspace и API key
3. Раздать URL команде: `https://mcp.plane.company.com:9000/sse`
4. Все подключаются к одному серверу

```json
{
  "servers": {
    "plane": {
      "url": "https://mcp.plane.company.com:9000/sse"
    }
  }
}
```

## 🔐 Безопасность

### stdio (Локальный)
- ✅ API ключи хранятся локально в конфигурации
- ✅ Никакой передачи через сеть
- ⚠️ Каждый пользователь должен иметь свой API ключ

### SSE (Удалённый)
- ⚠️ API ключи хранятся на сервере
- ⚠️ Endpoint должен быть защищён (VPN, authentication)
- ✅ Централизованное управление доступом
- ✅ Можно использовать один мощный API ключ для команды

**Рекомендации для SSE:**
1. Используйте HTTPS (не HTTP)
2. Настройте authentication/authorization
3. Ограничьте доступ по IP (whitelist)
4. Используйте reverse proxy (Traefik, Nginx)
5. Логируйте все запросы

## 🚀 Развёртывание

### Локальное (stdio)
```bash
# Пользователь просто запускает
npx -y @mikey-semy/plane-mcp
```

### Удалённое (SSE)

**Docker Compose:**
```yaml
version: '3.8'
services:
  plane-mcp:
    image: plane-mcp:latest
    ports:
      - "9000:9000"
    environment:
      - PLANE_API_KEY=plane_xxx
      - PLANE_WORKSPACE_SLUG=workspace
      - PLANE_API_HOST_URL=https://api.plane.so/
      - MCP_TRANSPORT=sse
      - MCP_HOST=0.0.0.0
      - MCP_PORT=9000
```

**Dokploy/Traefik:**
```yaml
labels:
  - "traefik.enable=true"
  - "traefik.http.routers.mcp.rule=Host(`mcp.plane.company.com`)"
  - "traefik.http.services.mcp.loadbalancer.server.port=9000"
```

## 📊 Производительность

### stdio
- **Latency**: ~10-50ms (локальный процесс)
- **Startup**: 2-3 секунды (запуск Python + uv)
- **Memory**: ~50-100MB (Python процесс)
- **CPU**: Низкая нагрузка

### SSE
- **Latency**: зависит от сети (обычно 50-200ms)
- **Startup**: мгновенно (сервер уже работает)
- **Memory**: ~100-200MB (постоянный процесс)
- **CPU**: Низкая нагрузка (многопоточность)

## 🔧 Troubleshooting

### stdio не работает

**Проблема:** `Error: uv is not installed`

**Решение:**
```bash
# Windows
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"

# macOS/Linux
curl -LsSf https://astral.sh/uv/install.sh | sh
```

### SSE не подключается

**Проблема:** `Connection failed: ERR_CONNECTION_REFUSED`

**Проверьте:**
1. Сервер запущен: `docker ps`
2. Порт открыт: `netstat -an | grep 9000`
3. Firewall: разрешён входящий трафик
4. URL правильный: `curl http://server:9000/sse`

**Проблема:** `SSL certificate error`

**Решение:**
- Используйте Let's Encrypt для SSL сертификата
- Или используйте HTTP (небезопасно!)
- Настройте Traefik для автоматических сертификатов
